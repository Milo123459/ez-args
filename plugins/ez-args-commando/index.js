const ez = require('../../index');
/**
 * @param {ez} parser 
 * @param {Object} options 
 */
module.exports=(parser,options) => {
    this.parser=parser;
    this.options=options;
    return {
        
    }
}
new Map()